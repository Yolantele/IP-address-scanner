{
  "paths": {
    "/api/maps": {
      "post": {
        "authRequired": false,
        "summary": "Creates row for number of devices per network",
        "parameters": [
          {"name": "ssid", "required": true, "type": "string", "description": "network name/SSID" },
          {"name": "devices", "required": true, "type": "string", "description": "number of devices on that netork" },
          {"name": "building", "required": true, "type": "string", "description": "building the netwrok belongs to or is close proximity" }
        ]
      }
    },
    "/api/scan": {
      "post": {
        "authRequired": false,
        "summary": "posts network scan maps to server",
        "parameters": []
      }
    },
     "/api/hello": {
      "get": {
        "authRequired": false,
        "summary": "Hello from the network-scanner server"
      }
    }
  }
}
